<%- include('partials/header'); -%>

    <h1>Home</h1>

<p> <%= startingContent %> </p>
<!-- <% for(var i=0;i<postsArr.length;i++){  %>
    <div class="item">
        <h1><%= postsArr[i].title %> </h1>
        <p><%= postsArr[i].content %> </p>
    </div>

<% } %> -->

<% postsArr.forEach(function (post){ %>
    <div class="item">
        <h1><%= post.title %> </h1>
        <% if((post.content).length > 200) { %>
            <% const cont = (post.content).substring(0,200) + " ..." %>
            <p><%= cont %> 
            <a href="/posts/<%= post.title %>"><button class="btn btn-primary btn-sm">read here</button></a> </p>
        <% } else {%>
            <p><%= post.content %> </p>
        <% } %>
    </div>
<% }) %>

<div class="plus">
    <a href="/compose"><i class="fa-solid fa-plus plus-sign"></i></a>
</div>

<%- include('partials/footer'); -%>